open_clip_torch>=2.32      # CLIP RN50 image + text towers (zero-shot branch)
flow_matching>=1.0         # Meta's reference probability paths and ODE solvers (stage 2)
torchdiffeq>=0.2.4         # ODE backend behind flow_matching.solver.ODESolver
